{
  "name": "garron",
  "version": "0.1.0",
  "private": true,
  "description": "Garrón — monorepo: pipeline de ingesta (raíz) + frontend Next.js (workspace web/). La Edge Function vive en consulta/ (Deno, sin npm).",
  "type": "module",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "ingesta": "node ingesta/cargar.js",
    "dev": "npm run dev --workspace web",
    "build:web": "npm run build --workspace web"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "@xenova/transformers": "^2.17.2",
    "cheerio": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
